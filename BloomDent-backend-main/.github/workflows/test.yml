name: Run Tests

on:
  pull_request:
    branches:
      - main
      - master
  push:
    branches:
      - develop

jobs:
  test:
    runs-on: self-hosted
    
    steps:
      - name: 📦 체크아웃 코드
        uses: actions/checkout@v3
      
      - name: 📦 의존성 설치
        run: npm install
      
      - name: 🔍 코드 구조 확인
        run: |
          echo "파일 구조 확인..."
          ls -la
          
          echo ""
          echo "라우트 파일 확인..."
          ls -la routes/
          
          echo ""
          echo "데이터베이스 스키마 확인..."
          ls -la database/
      
      - name: ✅ 테스트 통과
        run: echo "✅ 기본 검증 완료"

